# Evaluation — TASK-226

`RetryGuard` reduces retry-loop friction (the top signal from Self-Improver TASK-225: 165 occurrences). Deterministic, fail-closed, provenance-bearing (signature + count + message). Directly enforces AGENTS.md §12 auto-stop rule in code.
