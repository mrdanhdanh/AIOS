# Test — TASK-226

7 unit tests in `aios/runtime/tests/test_retry_guard.py`:
- threshold default, auto-stop at threshold, independent signatures, report root cause, reset clears, invalid threshold, empty signature rejected.
