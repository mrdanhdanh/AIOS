# Regression — TASK-105

- Dependency closure: T001..T104. Chạy `python -m pytest aios -q`.
- Chỉ thêm `aios/independent_harness/oracle.py` (mới). Không sửa module cũ.
- **Result: PASS** — không regression.
