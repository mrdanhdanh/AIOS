# TASK-136 — Critique 2

## Refinement
- Đồng ý Critique 1: `isolate` fail-closed khi thiếu `policy_ref`.
- Thêm test: tạo id trùng -> REJECT (T001 Rule 1).
- `provenance()` phải sinh `content_hash` ổn định.

## Verdict
APPROVED — sẵn sàng BREAKDOWN.
