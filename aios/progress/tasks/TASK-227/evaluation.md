# Evaluation — TASK-227

`StubGuard` enforces AGENTS.md §12 "cấm null-stub / bước SKIPPED" in code. Reduces the 44 skipped-stub signals surfaced by Self-Improver TASK-225. Deterministic, fail-closed, provenance-bearing (step_id + status + detail).
