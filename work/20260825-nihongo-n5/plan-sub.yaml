# Sub-plan generated by the AIOS Practical Planner Loop (Flow I, T220-224).
# Produces a REAL artifact (delivery manifest) proving the loop delivered output.
workflow:
  name: Generated Sub-Task - N5 Site Delivery Manifest
  version: 0.1.0
  permissions: [process.execute]
  nodes:
    - id: write-manifest
      type: task
      command: python -c "import json, os; from datetime import datetime, timezone; root=r'D:\AIOS\work\20260825-nihongo-n5'; files=['index.html','css/style.css','js/data.js','js/app.js','README.md']; manifest={'artifact':'nihongo-n5-website','generated_by':'AIOS Practical Planner Loop (T220-224)','files':files,'sizes':{f:os.path.getsize(os.path.join(root,f)) for f in files},'timestamp':datetime.now(timezone.utc).isoformat()}; p=os.path.join(root,'delivery-manifest.json'); open(p,'w',encoding='utf-8').write(json.dumps(manifest,indent=2,ensure_ascii=False)); print('MANIFEST_WRITTEN:', p); print('FILES:', len(files))"
