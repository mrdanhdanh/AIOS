# TASK-226 — Deterministic Auto-Stop / RetryGuard
# Plan generated by AIOS Planner (2026-08-25). Drives the full governance
# pipeline + 7 gates for TASK-226 via the real Orchestrator/UnifiedTaskGate.
# NOTE: run only when terminal is enabled.
workflow:
  name: TASK-226 — Deterministic Auto-Stop / RetryGuard (governance pipeline + 7 gates)
  version: 0.1.0
  permissions: [process.execute]
  nodes:
    # Flow A — Coordinator Agent + 7 Governance Gates: run the real pipeline for TASK-226.
    - id: flow-a-gates
      type: task
      command: python d:\AIOS\scripts\run_task.py TASK-226 --job-dir d:\AIOS\work\20260825-task226\logs

    # Flow C — Architecture Guard: verify no ARCH-001..004 violations in implementation.
    - id: flow-c-arch
      type: task
      command: python -c "import os, sys, subprocess; os.chdir(r'd:\AIOS'); sys.exit(subprocess.run([sys.executable,'-m','pytest','aios/governance/architecture','-q']).returncode)"

    # Flow D — Local CI: run the unified gate check for TASK-226 (fail-closed).
    - id: flow-d-ci
      type: task
      command: python -c "import os, sys, subprocess; os.chdir(r'd:\AIOS'); sys.exit(subprocess.run([sys.executable,r'aios/governance/cli/gate_check.py','--task','TASK-226']).returncode)"

    # Tests: RetryGuard unit tests (deterministic / fail-closed / independent signatures).
    - id: run-tests
      type: task
      command: python -c "import os, sys, subprocess; os.chdir(r'd:\AIOS'); sys.exit(subprocess.run([sys.executable,'-m','pytest','aios/runtime/tests/test_retry_guard.py','-q']).returncode)"

    # Full suite regression.
    - id: full-suite
      type: task
      command: python -c "import os, sys, subprocess; os.chdir(r'd:\AIOS'); sys.exit(subprocess.run([sys.executable,'-m','pytest','aios','-q']).returncode)"

    # Proof: durable log written by run_task.py (EvidenceStore is in-memory only).
    - id: verify-log
      type: task
      command: python -c "import os, glob; fs=glob.glob(r'd:\AIOS\work\20260825-task226\logs\task-TASK-226-*.json'); print('LOG FILES:', fs); assert fs, 'no durable log written'; print('OK durable log present')"
