# This file represents the output of the AIOS Planner Agent for the request:
#   "Create a status report file in work/20260825-planner-demo/output/"
# In a real run, the Planner Agent (T223) would generate this into work/YYYYMMDD-slug/.
workflow:
  name: Generated Sub-Task - Write Report
  version: 0.1.0
  permissions: [process.execute]
  nodes:
    - id: write-report
      type: task
      command: python -c "from pathlib import Path; p = Path(r'D:\AIOS\work\20260825-planner-demo\output\report.md'); p.parent.mkdir(parents=True, exist_ok=True); p.write_text('# AIOS Planner Demo Report\n\nGenerated by the Practical AIOS Loop (Planner -> plan.yaml -> Real Executor).\n', encoding='utf-8'); print('REPORT_WRITTEN:', p)"
