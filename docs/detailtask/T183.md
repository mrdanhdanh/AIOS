## TASK-183 — Approval Workflow + Rollback Recommendation

**Mục tiêu**

Triển khai **Approval Workflow + Rollback Recommendation** như một năng lực có contract, evidence và harness riêng — workflow phê duyệt có kiểm soát + đề xuất rollback, fail-closed. TASK-183 là **workflow + recommender, không phải recovery mới** (dựa trên Trust Lifecycle T182 + Permission/Human Approval T097 + Autonomous Recovery T055 + Evidence T001).

TASK-183 thuộc **M24**, ngay sau T182. Trọng tâm canonical: **Approval Workflow + Rollback Recommendation**.

### 1. Phạm vi — Workflow + recommender, không recovery mới

```text
Approval Workflow + Rollback Recommendation
├── Approval Workflow    (phê duyệt có duyệt)
├── Rollback Rec         (đề xuất rollback)
├── Audit                (mọi approval có audit)
└── Policy Boundary      (T113)
```

* **Approval Workflow** — workflow phê duyệt có approval (T097).
* **Rollback Rec** — đề xuất rollback khi trust invalidate (T182/T055).
* **Audit** — mọi approval có audit trail (T001 Rule 5).
* **Policy Boundary** — workflow qua policy (T113).

### 2. Approval Workflow + Rollback Recommendation Contract

```text
ApprovalRecord
├── approval_id          (immutable, T001 Rule 1)
├── trust_ref            (T182)
├── approver
├── rollback_recommended: bool
├── audit_ref            (T001)
└── authority: aios
```

Quy tắc:

* Approval thiếu approver → không active (fail-closed, T097/T113).
* Trust INVALIDATED → rollback_recommended=true (T182/T055).
* Mọi approval có provenance/audit (T001 Rule 5).
* Workflow deterministic: cùng input → cùng verdict.

### 3. Approval Workflow + Rollback Recommendation Safety

* **Fail-closed** — thiếu approver → không active (T097/T113).
* **Immutable id** — `approval_id` không tái sử dụng (T001 Rule 1).
* **Evidence required** — mọi approval có audit (T001 Rule 5).
* **Deterministic** — cùng input → cùng verdict.
* **No parallel workflow** — dùng Trust Lifecycle (T182) + Permission/Human Approval (T097) + Recovery (T055) + Evidence (T001).

### 4. Deliverables

* Approval Workflow + Rollback Recommendation (workflow + recommend + audit).
* Policy Boundary (T113).
* Integration với Trust Lifecycle (T182) + Permission/Human Approval (T097) + Autonomous Recovery (T055) + Evidence (T001).
* Unit + Contract + Integration + Architecture + Regression tests.
* ADR/docs (bắt buộc — approval workflow + rollback recommendation).

### 5. Acceptance Criteria

TASK-183 chỉ PASS khi:

* Workflow phê duyệt có approval (T097).
* Trust INVALIDATED → rollback_recommended (T182/T055).
* Mọi approval có audit trail (T001 Rule 5).
* Thiếu approver → không active (fail-closed, T097/T113).
* Cùng input → cùng verdict (deterministic).
* Tích hợp được với Trust Lifecycle + Permission/Human Approval + Recovery + Evidence.
* Regression của các milestone trước PASS; không vi phạm invariants.

### 6. Dependency / Gate

```text
TASK-182 Trust Lifecycle + Invalidation + Selective Reverification
TASK-097 Permission + Human Approval + Apply + Re-test + Rollback + Certification
TASK-055 Autonomous Recovery
        ↓
TASK-183 Approval Workflow + Rollback Recommendation
        ↓
TASK-184 Quality Dashboard + Governance Harness
```

### 7. Test Matrix

| Scenario | Expected |
| -------- | -------- |
| approval có approver | active |
| thiếu approver | không active (fail-closed) |
| trust INVALIDATED | rollback_recommended=true |
