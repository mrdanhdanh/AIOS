## TASK-184 — Quality Dashboard + Governance Harness

**Mục tiêu**

Triển khai **Quality Dashboard + Governance Harness** như một năng lực có contract, evidence và harness riêng — dashboard chất lượng + governance harness tích hợp toàn bộ M24 (T175→T183). TASK-184 là **dashboard + harness tích hợp, không phải gate mới** (dựa trên toàn bộ M24 T175→T183 + Dashboard T072 + Observability T021 + Evidence T001).

TASK-184 thuộc **M24**, là task cuối của milestone. Trọng tâm canonical: **Quality Dashboard + Governance Harness**.

### 1. Phạm vi — Dashboard + harness tích hợp, không gate mới

```text
Quality Dashboard + Governance Harness
├── Dashboard             (hiển thị chất lượng)
├── Harness               (tích hợp T175→T183)
├── Observability         (metric từ T021)
└── Policy Boundary       (T113)
```

* **Dashboard** — hiển thị chất lượng từ gate/debt/trust (T175/T179/T182).
* **Harness** — harness chạy toàn bộ governance M24 end-to-end.
* **Observability** — metric từ Observability (T021).
* **Policy Boundary** — mọi run qua policy (T113).

### 2. Quality Dashboard + Governance Harness Contract

```text
GovernanceHarnessRun
├── run_id               (immutable, T001 Rule 1)
├── gate_ref             (T175)
├── ledger_ref           (T181)
├── dashboard_view
├── evidence_ref         (T001)
└── authority: aios
```

Quy tắc:

* Harness chạy toàn bộ governance M24 (T175→T183) end-to-end.
* Mọi run có provenance (T001 Rule 5).
* Run deterministic: cùng input → cùng output (T029/T079).
* Run không vượt policy boundary (T113).

### 3. Quality Dashboard + Governance Harness Safety

* **Fail-closed** — governance fail → harness báo FAIL, không promote (T078).
* **Immutable id** — `run_id` không tái sử dụng (T001 Rule 1).
* **Evidence required** — mọi run có provenance (T001 Rule 5).
* **Deterministic** — cùng input → cùng output (T029/T079).
* **No parallel harness** — dùng toàn bộ M24 (T175→T183) + Dashboard (T072) + Observability (T021) + Evidence (T001).

### 4. Deliverables

* Quality Dashboard + Governance Harness (dashboard + harness).
* Policy Boundary (T113).
* Integration với toàn bộ M24 (T175→T183) + Dashboard (T072) + Observability (T021) + Evidence (T001).
* Unit + Contract + Integration + Architecture + Regression tests.
* ADR/docs (bắt buộc — quality dashboard + governance harness).

### 5. Acceptance Criteria

TASK-184 chỉ PASS khi:

* Dashboard hiển thị được chất lượng (T175/T179/T182).
* Harness chạy toàn bộ governance M24 (T175→T183) end-to-end.
* Mọi run có provenance (T001 Rule 5).
* Cùng input → cùng output (deterministic, T029/T079).
* Tích hợp được với toàn bộ M24 + Dashboard + Observability + Evidence.
* Regression của các milestone trước PASS; không vi phạm invariants.

### 6. Dependency / Gate

```text
TASK-175 Quality Gate + Gate States
TASK-176 Risk Model + Classification
TASK-177 Policy Engine + Profiles + Precedence
TASK-178 Exception Management
TASK-179 Quality Debt Tracking
TASK-180 Release Gate + Decision Explainability
TASK-181 Governance Ledger + Provenance Graph
TASK-182 Trust Lifecycle + Invalidation + Selective Reverification
TASK-183 Approval Workflow + Rollback Recommendation
TASK-072 AIOS Dashboard 1.0
TASK-021 Observability
        ↓
TASK-184 Quality Dashboard + Governance Harness
```

TASK-184 là task cuối của M24 — tích hợp toàn bộ governance & quality.

### 7. Test Matrix

| Scenario | Expected |
| -------- | -------- |
| chạy governance end-to-end | PASS (T175→T183) |
| governance fail | harness FAIL (fail-closed, T078) |
| cùng input | cùng output (deterministic, T029) |
