Tiếp tục **TASK-035 — Identity + Principal + RBAC/ABAC**.

Theo roadmap, TASK-035 thuộc **M7 Enterprise**, là nền tảng bắt buộc trước TASK-036 Multi-Tenancy. Master specification hiện chỉ định nghĩa mục tiêu ở mức ngắn; vì vậy phần dưới là **structured expansion**, không phải lịch sử implementation. 

## 1. Mục tiêu

Xây **Identity & Authorization Foundation** cho AIOS để mọi execution có thể xác định:

`Who → Tenant → Role/Attributes → Action → Resource → Policy → Decision`

Không thay thế `Permission Service` hoặc `Policy Service`; TASK-035 bổ sung **identity context** làm input cho chúng.

Invariant mới của M7:

* **INV-022 — Identity First:** mọi execution phải có `Principal`.
* Authorization phải **fail-closed** khi identity/authorization context không đầy đủ.

---

## 2. Scope

### 2.1 Principal Contract

Chuẩn hóa 5 loại principal:

```text
User
Service
Agent
Workflow
System
```

Contract tối thiểu:

```yaml
principal:
  id: user-123
  type: user
  tenant_id: company-a
  roles:
    - developer
  attributes:
    department: IT
    environment: development
```

Các field chính:

| Field         | Ý nghĩa            |
| ------------- | ------------------ |
| `id`          | định danh duy nhất |
| `type`        | loại principal     |
| `tenant_id`   | tenant sở hữu      |
| `roles`       | RBAC roles         |
| `attributes`  | dữ liệu cho ABAC   |
| `metadata`    | thông tin mở rộng  |
| `auth_source` | nguồn xác thực     |

`tenant_id` được thiết kế ngay từ TASK-035 để TASK-036 có thể mở rộng tenant boundary mà không phải thay đổi Principal Contract.

---

## 3. Identity Context

Mỗi request/execution phải mang identity context:

```text
Request
  ↓
Identity Resolver
  ↓
Principal
  ↓
Authorization Context
  ↓
Policy / Permission
  ↓
Execution
```

Không cho phép component tự suy đoán identity từ request payload.

### Authorization Context

Nên chứa tối thiểu:

```text
principal
tenant
roles
attributes
resource
action
environment
delegation
requested_capabilities
```

Ví dụ:

```yaml
authorization:
  principal: user-123
  tenant: company-a
  action: filesystem.write
  resource: project-x/src/main.py
  environment: development
```

---

## 4. RBAC

RBAC xử lý quyền theo role.

Ví dụ:

```yaml
roles:
  developer:
    permissions:
      - filesystem.read
      - filesystem.write
      - git.read
      - git.write

  reviewer:
    permissions:
      - filesystem.read
      - git.read
      - review.execute
```

Không hard-code role vào Agent hoặc Tool.

Luồng:

```text
Principal
 → Role Resolver
 → Permission Set
 → Policy Engine
```

---

## 5. ABAC

RBAC không đủ cho enterprise.

ABAC phải hỗ trợ ít nhất:

```text
Subject
Resource
Action
Environment
```

Ví dụ:

```text
subject.department == "IT"
AND
resource.environment == "development"
AND
action == "filesystem.write"
```

→ `ALLOW`

Trong production:

```text
subject.department == "IT"
AND
resource.environment == "production"
AND
action == "filesystem.write"
```

→ có thể `DENY` hoặc `ASK` tùy Policy.

Điểm quan trọng: **ABAC evaluation thuộc authorization/policy layer**, không nằm trong Agent.

---

## 6. Delegation

AIOS có Agent và Workflow identity, nên cần hỗ trợ chuỗi:

```text
User
 ↓ delegates
Orchestrator
 ↓ delegates
Coder Agent
 ↓ requests
Capability
 ↓
Tool
```

Authorization phải giữ được **provenance của delegation**.

Ví dụ:

```yaml
delegation:
  actor: user-123
  delegate: agent-coder
  scope:
    - filesystem.read
    - git.read
  expires_at: ...
```

### Capability attenuation

Agent **không được nhận nhiều quyền hơn principal đã cấp**.

```text
User permissions
        ↓
Delegation
        ↓
Agent permissions
        ↓
Capability permissions
        ↓
Tool execution
```

Luôn:

`effective_permissions ⊆ delegated_permissions ⊆ principal_permissions`

---

## 7. Authorization Decision

Chuẩn hóa decision:

```text
ALLOW
DENY
ASK
```

Decision phải có reason và provenance:

```yaml
decision:
  result: DENY
  reason: role_missing
  principal: user-123
  action: filesystem.write
  resource: production/repo
  policy_version: policy-1.4
```

`UNKNOWN` không được tự động chuyển thành `ALLOW`.

---

## 8. Architecture Boundary

TASK-035 phải giữ nguyên architecture hiện tại:

```text
User / Service / Agent
        ↓
     Identity
        ↓
 Authorization
        ↓
 Policy / Permission
        ↓
 Orchestrator
        ↓
 Runtime
        ↓
 Capability
        ↓
 Tool
```

Không cho:

```text
Agent → Identity DB
Agent → Role DB
Agent → Policy DB
Agent → Credential
```

Agent chỉ nhận **authorization context/effective capability**, không tự thực hiện authorization.

---

## 9. Deliverables

### Contracts

```text
Principal
IdentityContext
Role
Permission
Attribute
AuthorizationRequest
AuthorizationDecision
Delegation
```

### Components

```text
Identity Registry
Identity Resolver
Role Resolver
Attribute Resolver
Authorization Engine
Delegation Manager
```

### Tests

* Principal contract tests
* Identity resolution tests
* RBAC tests
* ABAC tests
* delegation tests
* capability attenuation tests
* fail-closed tests
* architecture tests
* regression tests

### Documentation

* ADR nếu thay đổi architectural boundary
* Identity/Authorization contract documentation
* threat/security model cơ bản

---

# 10. Breakdown

### TASK-035.1 — Principal Contract

* [ ] Define `Principal`
* [ ] Define principal types
* [ ] Define immutable identity fields
* [ ] Validate `tenant_id` compatibility
* [ ] Contract tests

### TASK-035.2 — Identity Resolver

* [ ] Resolve authenticated identity
* [ ] Construct `IdentityContext`
* [ ] Reject missing/invalid identity
* [ ] Prevent caller-controlled identity spoofing
* [ ] Tests

### TASK-035.3 — RBAC

* [ ] Role contract
* [ ] Permission contract
* [ ] Role → permission resolution
* [ ] deny-by-default
* [ ] tests

### TASK-035.4 — ABAC

* [ ] Attribute model
* [ ] Subject/resource/action/environment model
* [ ] deterministic policy evaluation
* [ ] deny/ask/allow
* [ ] tests

### TASK-035.5 — Delegation

* [ ] Delegation contract
* [ ] scope restriction
* [ ] expiration
* [ ] provenance
* [ ] capability attenuation
* [ ] tests

### TASK-035.6 — Runtime Integration

* [ ] Inject Principal vào execution context
* [ ] Integrate Policy Service
* [ ] Integrate Permission Service
* [ ] Preserve existing Runtime boundary
* [ ] Regression

### TASK-035.7 — Architecture & Security Gate

* [ ] INV-022 enforcement
* [ ] Agent direct-identity-access test
* [ ] spoofing tests
* [ ] privilege-escalation tests
* [ ] fail-closed tests
* [ ] full regression

---

# 11. Acceptance Criteria

TASK-035 chỉ PASS khi:

1. Mọi execution hợp lệ đều có `Principal`.
2. Principal hỗ trợ `User / Service / Agent / Workflow / System`.
3. RBAC resolve permission deterministic.
4. ABAC evaluate được Subject/Resource/Action/Environment.
5. Authorization mặc định **deny** khi thiếu thông tin cần thiết.
6. Delegation không thể cấp quyền vượt principal.
7. Agent không truy cập trực tiếp Identity/Role/Policy storage.
8. Authorization decision có reason + provenance.
9. INV-022 được architecture test enforce.
10. Regression của M0–M6 PASS.
11. Không tạo control plane song song.
12. Evidence của các authorization decision có thể truy xuất.

Roadmap xác định TASK-035 phải đi **trước TASK-036**, vì Identity + Tenant cần được đặt trước khi mở rộng execution boundary sang distributed/enterprise runtime. 

## 12. Gate của TASK-035

```text
Principal Contract
      ↓
Identity Resolution
      ↓
RBAC + ABAC
      ↓
Delegation
      ↓
Policy / Permission Integration
      ↓
Architecture Verification
      ↓
Regression
      ↓
PASS
      ↓
TASK-036
```

**Trạng thái đề xuất:** `IN-PROGRESS / SPECIFICATION PHASE`

Chưa được coi là `DONE`; theo workflow bắt buộc, task còn phải qua **SPEC → CRITIQUE ×2 → BREAKDOWN → REVIEW → IMPLEMENT → TEST → EVALUATE → REGRESSION → COMMIT**. 
