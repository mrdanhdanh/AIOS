## TASK-212 — Coding Doctor

**Mục tiêu**

Triển khai **Coding Doctor** như một năng lực có contract, evidence và harness riêng — chẩn đoán sức khỏe coding, fail-closed. TASK-212 là **doctor, không phải observability mới** (dựa trên Repo KG Integration T211 + Doctor/Readiness T034 + Observability T021 + Evidence T001).

TASK-212 thuộc **M26**, ngay sau T211. Trọng tâm canonical: **Coding Doctor**.

### 1. Phạm vi — Doctor, không observability mới

```text
Coding Doctor
├── Diagnose             (chẩn đoán coding)
├── Readiness            (kiểm tra readiness, T034)
├── Observability Bind   (gắn observability T021)
└── Policy Boundary      (T113)
```

* **Diagnose** — chẩn đoán sức khỏe coding.
* **Readiness** — kiểm tra readiness từ Doctor/Readiness (T034).
* **Observability Bind** — dùng Observability (T021).
* **Policy Boundary** — doctor qua policy (T113).

### 2. Coding Doctor Contract

```text
CodingDoctor
├── doctor_id            (immutable, T001 Rule 1)
├── integ_ref            (T211)
├── readiness_ref        (T034)
├── diagnosis
├── evidence_ref         (T001)
└── authority: aios
```

Quy tắc:

* Mọi diagnosis có provenance (T001 Rule 5).
* Diagnosis deterministic: cùng state → cùng verdict.
* `doctor_id` immutable (T001 Rule 1).
* Readiness fail → không promote (fail-closed, T034).

### 3. Coding Doctor Safety

* **Fail-closed** — readiness fail → không promote (T034).
* **Immutable id** — `doctor_id` không tái sử dụng (T001 Rule 1).
* **Evidence required** — mọi diagnosis có provenance (T001 Rule 5).
* **Deterministic** — cùng state → cùng verdict.
* **No parallel doctor** — dùng Repo KG Integration (T211) + Doctor/Readiness (T034) + Observability (T021) + Evidence (T001).

### 4. Deliverables

* Coding Doctor (diagnose + readiness + observability bind).
* Policy Boundary (T113).
* Integration với Repo KG Integration (T211) + Doctor/Readiness (T034) + Observability (T021) + Evidence (T001).
* Unit + Contract + Integration + Architecture + Regression tests.
* ADR/docs (bắt buộc — coding doctor).

### 5. Acceptance Criteria

TASK-212 chỉ PASS khi:

* Chẩn đoán được sức khỏe coding.
* Kiểm tra được readiness (T034).
* Readiness fail → không promote (fail-closed, T034).
* Mọi diagnosis có provenance (T001 Rule 5).
* Cùng state → cùng verdict (deterministic).
* Tích hợp được với Repo KG Integration + Doctor/Readiness + Observability + Evidence.
* Regression của các milestone trước PASS; không vi phạm invariants.

### 6. Dependency / Gate

```text
TASK-211 Repository Knowledge Graph Integration
TASK-034 Doctor + Readiness
TASK-021 Observability
        ↓
TASK-212 Coding Doctor
        ↓
TASK-213 Coding Health Score
```

### 7. Test Matrix

| Scenario | Expected |
| -------- | -------- |
| diagnose có provenance | verdict |
| readiness fail | không promote (fail-closed) |
| cùng state | cùng verdict (deterministic) |
