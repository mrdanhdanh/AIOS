## TASK-093 — Behavioral Spec + ADR-0008

**Mục tiêu**

Soạn **Behavioral Spec + ADR-0008** — chuẩn hóa tài liệu behavioral conformance (T089) và quyết định kiến trúc liên quan thành ADR-0008, đóng gói toàn bộ chuỗi M13. TASK-093 là **documentation + ADR, không phải code mới**.

TASK-093 thuộc **M13**, ngay sau T092. Trọng tâm canonical: **Behavioral Spec + ADR-0008**.

### 1. Phạm vi — Documentation + ADR, không code

```text
Behavioral Spec + ADR-0008
├── Behavioral Spec doc   (T089 scenarios)
├── ADR-0008              (behavioral conformance decision)
├── Coverage/Trust refs   (T090/T091/T092)
└── Reference             (developer docs T071)
```

* **Behavioral Spec doc** — tài liệu hóa kịch bản hành vi (T089).
* **ADR-0008** — ghi nhận quyết định behavioral conformance + rationale.
* **References** — link tới Coverage/Trust/Meta (T090-T092).
* **Reference** — docs cho developer (T071).

### 2. Spec/ADR Contract

```text
BehaviorDoc
├── doc_id
├── covers: [T089, T090, T091, T092]
├── adr_ref: ADR-0008
├── status: PUBLISHED
└── evidence_ref
```

Quy tắc:

* Docs phải cover đủ T089-T092.
* ADR-0008 phải ghi rationale.
* Mọi doc reference có provenance (link task).

### 3. Spec/ADR Safety

* **Coverage** — docs cover đủ chuỗi M13.
* **ADR integrity** — ADR-0008 ghi rationale, không mâu thuẫn T089-T092.
* **No stale doc** — doc khớp implementation đã DONE.
* **Deterministic review** — cùng nội dung → cùng review result.
* **No parallel doc system** — dùng docs/ + ADR convention.

### 4. Deliverables

* Behavioral Spec doc (T089 scenarios).
* ADR-0008 (behavioral conformance decision + rationale).
* References tới Coverage/Trust/Meta.
* Reference docs cho developer (T071).
* Integration với Docs (docs/) + DX (T071) + chuỗi T089-T092.
* Review + link checks.
* ADR/docs (bắt buộc — task là docs).

### 5. Acceptance Criteria

TASK-093 chỉ PASS khi:

* Docs cover đủ T089-T092.
* ADR-0008 ghi rationale rõ ràng.
* Doc khớp implementation đã DONE (không stale).
* Mọi doc reference có provenance (link task).
* Cùng nội dung → cùng review result (deterministic).
* Tích hợp được với Docs + DX + chuỗi M13.
* Regression của các milestone trước PASS; không vi phạm invariants.

### 6. Dependency / Gate

```text
TASK-092 System Readiness vs Harness Trust
        ↓
TASK-093 Behavioral Spec + ADR-0008
        ↓
TASK-094 Detect + Diagnose (M14)
```

TASK-093 đóng M13; T094 mở M14 (Remediation).

### 7. Test Matrix

| Scenario | Expected |
| -------- | -------- |
| docs cover T089-T092 | PASS |
| ADR-0008 thiếu rationale | bị chặn |
| doc stale vs impl | bị chặn |
| doc link task | provenance đầy đủ |
| cùng nội dung | cùng review (deterministic) |
| doc reference valid | không 404 |

### 8. Definition of Done

```text
PLAN → SPEC → CRITIQUE×2 → BREAKDOWN → REVIEW
→ IMPLEMENT → TEST → EVALUATE → REGRESSION → PROGRESS/LOG → COMMIT
```
