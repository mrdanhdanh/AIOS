## TASK-163 — Evidence Collector + Evidence Integrity

**Mục tiêu**

Triển khai **Evidence Collector + Evidence Integrity** như một năng lực có contract, evidence và harness riêng — thu thập evidence xuyên suốt và bảo đảm integrity (hash/provenance), fail-closed. TASK-163 là **collector + integrity, không phải store mới** (dựa trên Requirement→Evidence Mapping T155 + Output/Artifact Collector T141 + Verification Integrity T078 + Evidence T001 Rule 5).

TASK-163 thuộc **M22**, ngay sau T162. Trọng tâm canonical: **Evidence Collector + Evidence Integrity**.

### 1. Phạm vi — Collector + integrity, không store mới

```text
Evidence Collector + Evidence Integrity
├── Collect              (thu evidence từ run, T141)
├── Hash                 (content_hash sha256, T078)
├── Provenance           (chain T001 Rule 5)
└── Policy Boundary      (T113)
```

* **Collect** — thu evidence từ mọi run (T141).
* **Hash** — mọi evidence có `content_hash` (T078).
* **Provenance** — mọi evidence có provenance chain (T001 Rule 5).
* **Policy Boundary** — collection qua policy (T113).

### 2. Evidence Collector + Evidence Integrity Contract

```text
CollectedEvidence
├── evidence_id          (immutable, T001 Rule 1)
├── map_ref              (T155)
├── content_hash         (sha256, T078)
├── producer             (collector)
├── evidence_chain[]     (T001 Rule 5)
├── integrity_verified   (T078)
└── authority: aios
```

Quy tắc:

* Mọi evidence có `content_hash` (T078) + provenance chain (T001 Rule 5).
* Evidence không verify → không promote PASS (fail-closed, T078).
* `evidence_id` immutable (T001 Rule 1).
* Cùng evidence + cùng verifier → cùng verdict (deterministic).

### 3. Evidence Collector + Evidence Integrity Safety

* **Fail-closed** — evidence không verify → không promote PASS (T078).
* **Integrity verified** — evidence qua integrity gate (T078).
* **Immutable id** — `evidence_id` không tái sử dụng (T001 Rule 1).
* **Evidence required** — mọi evidence có provenance chain (T001 Rule 5).
* **Deterministic** — cùng evidence + verifier → cùng verdict.
* **No parallel collector** — dùng Requirement→Evidence (T155) + Collector (T141) + Integrity (T078) + Evidence (T001).

### 4. Deliverables

* Evidence Collector + Evidence Integrity (collect + hash + provenance).
* Policy Boundary (T113).
* Integration với Requirement→Evidence Mapping (T155) + Output/Artifact Collector (T141) + Verification Integrity (T078) + Evidence (T001).
* Unit + Contract + Integration + Architecture + Regression tests.
* ADR/docs (bắt buộc — evidence collector + integrity).

### 5. Acceptance Criteria

TASK-163 chỉ PASS khi:

* Collector thu evidence có `content_hash` (T078).
* Mọi evidence có provenance chain đầy đủ (T001 Rule 5).
* Evidence không verify → không promote PASS (fail-closed, T078).
* `evidence_id` immutable (T001 Rule 1).
* Cùng evidence + verifier → cùng verdict (deterministic).
* Tích hợp được với Requirement→Evidence Mapping + Collector + Integrity + Evidence.
* Regression của các milestone trước PASS; không vi phạm invariants.

### 6. Dependency / Gate

```text
TASK-155 Requirement → Evidence Mapping
TASK-141 Output + Artifact Collector
TASK-078 Verification Integrity / Fail-Closed Gate
TASK-001 Evidence (Rule 5)
        ↓
TASK-163 Evidence Collector + Evidence Integrity
        ↓
TASK-164 Trust Evaluator + CodingCertificate + Verification Harness
```

### 7. Test Matrix

| Scenario | Expected |
| -------- | -------- |
| collect evidence có hash | integrity_verified |
| evidence thiếu provenance | reject (fail-closed) |
| cùng evidence + verifier | cùng verdict (deterministic) |
