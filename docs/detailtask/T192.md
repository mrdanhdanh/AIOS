## TASK-192 — Efficiency Evaluator

**Mục tiêu**

Triển khai **Efficiency Evaluator** như một năng lực có contract, evidence và harness riêng — đánh giá hiệu quả (latency/throughput/cost) của agent, fail-closed. TASK-192 là **efficiency evaluator, không phải verifier mới** (dựa trên Evaluation Engine T186 + Performance Verifier T161 + Quality Dimensions T187 + Evidence T001).

TASK-192 thuộc **M25**, ngay sau T191. Trọng tâm canonical: **Efficiency Evaluator**.

### 1. Phạm vi — Efficiency evaluator, không verifier mới

```text
Efficiency Evaluator
├── Metric Collect       (thu metric, T161)
├── Threshold            (ngưỡng SLA)
├── Verdict              (PASS/FAIL/INCONCLUSIVE)
└── Policy Boundary      (T113)
```

* **Metric Collect** — thu metric hiệu năng từ Performance Verifier (T161).
* **Threshold** — so sánh với ngưỡng SLA.
* **Verdict** — vượt ngưỡng → FAIL (fail-closed).
* **Policy Boundary** — evaluation qua policy (T113).

### 2. Efficiency Evaluator Contract

```text
EfficiencyEval
├── eval_id              (immutable, T001 Rule 1)
├── engine_ref           (T186)
├── metrics_ref          (T161)
├── verdict              (PASS/FAIL/INCONCLUSIVE)
├── evidence_ref         (T001)
└── authority: aios
```

Quy tắc:

* Vượt ngưỡng hiệu năng → FAIL (fail-closed, T078/T161).
* Mọi eval có provenance (T001 Rule 5).
* Evaluation deterministic: cùng input → cùng verdict.
* Metric không lộ secret (T040/T113).

### 3. Efficiency Evaluator Safety

* **Fail-closed** — vượt ngưỡng → FAIL (T078/T161).
* **Immutable id** — `eval_id` không tái sử dụng (T001 Rule 1).
* **Evidence required** — mọi eval có provenance (T001 Rule 5).
* **Deterministic** — cùng input → cùng verdict.
* **No parallel evaluator** — dùng Evaluation Engine (T186) + Performance Verifier (T161) + Quality Dimensions (T187) + Evidence (T001).

### 4. Deliverables

* Efficiency Evaluator (metric + threshold + verdict).
* Policy Boundary (T113).
* Integration với Evaluation Engine (T186) + Performance Verifier (T161) + Quality Dimensions (T187) + Evidence (T001).
* Unit + Contract + Integration + Architecture + Regression tests.
* ADR/docs (bắt buộc — efficiency evaluator).

### 5. Acceptance Criteria

TASK-192 chỉ PASS khi:

* Evaluator thu metric hiệu năng (T161).
* Vượt ngưỡng SLA → FAIL (fail-closed, T078/T161).
* Mọi eval có provenance (T001 Rule 5).
* Cùng input → cùng verdict (deterministic).
* Metric không lộ secret (T040/T113).
* Tích hợp được với Evaluation Engine + Performance Verifier + Quality Dimensions + Evidence.
* Regression của các milestone trước PASS; không vi phạm invariants.

### 6. Dependency / Gate

```text
TASK-186 Evaluation Engine
TASK-161 Performance Verifier
TASK-187 Quality Dimensions
        ↓
TASK-192 Efficiency Evaluator
        ↓
TASK-193 Failure Attribution
```

### 7. Test Matrix

| Scenario | Expected |
| -------- | -------- |
| trong ngưỡng | PASS |
| vượt ngưỡng | FAIL (fail-closed) |
| cùng input | cùng verdict (deterministic) |
