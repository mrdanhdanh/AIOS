## TASK-218 — Full M0–M26 Regression

**Mục tiêu**

Triển khai **Full M0–M26 Regression** như một năng lực có contract, evidence và harness riêng — chạy regression toàn bộ roadmap M0–M26, fail-closed. TASK-218 là **full regression, không phải integration mới** (dựa trên AIOS 2.0 Coding Integration T217 + Regression Gate T007/Rule 7 + Benchmark/Regression T033 + Evidence T001).

TASK-218 thuộc **M26**, là **task cuối của toàn bộ roadmap M0–M26**. Trọng tâm canonical: **Full M0–M26 Regression**.

### 1. Phạm vi — Full regression, không integration mới

```text
Full M0–M26 Regression
├── Regression Run        (chạy toàn bộ closure)
├── Gate                  (unified gate T001 Rule 7)
├── Report                (báo cáo coverage)
└── Policy Boundary       (T113)
```

* **Regression Run** — chạy test của toàn bộ dependency closure M0–M26.
* **Gate** — unified gate hội tụ 7 quy tắc (T001 Rule 7).
* **Report** — báo cáo coverage toàn bộ milestone.
* **Policy Boundary** — regression qua policy (T113).

### 2. Full M0–M26 Regression Contract

```text
FullRegression
├── run_id               (immutable, T001 Rule 1)
├── integ_ref            (T217)
├── closure[]            (T001→T217)
├── all_pass: bool
├── evidence_ref         (T001)
└── authority: aios
```

Quy tắc:

* Bất kỳ task trong closure FAIL → regression BLOCKED (fail-closed, T001 Rule 7).
* Mọi run có provenance (T001 Rule 5).
* Regression deterministic: cùng closure → cùng verdict.
* Closure không vượt policy boundary (T113).

### 3. Full M0–M26 Regression Safety

* **Fail-closed** — closure FAIL → BLOCKED (T001 Rule 7).
* **Immutable id** — `run_id` không tái sử dụng (T001 Rule 1).
* **Evidence required** — mọi run có provenance (T001 Rule 5).
* **Deterministic** — cùng closure → cùng verdict.
* **No parallel regression** — dùng AIOS 2.0 Coding Integration (T217) + Regression Gate (T001 Rule 7) + Benchmark/Regression (T033) + Evidence (T001).

### 4. Deliverables

* Full M0–M26 Regression (run + gate + report).
* Policy Boundary (T113).
* Integration với AIOS 2.0 Coding Integration (T217) + Regression Gate (T001 Rule 7) + Benchmark/Regression (T033) + Evidence (T001).
* Unit + Contract + Integration + Architecture + Regression tests.
* ADR/docs (bắt buộc — full M0–M26 regression).

### 5. Acceptance Criteria

TASK-218 chỉ PASS khi:

* Chạy được regression toàn bộ closure M0–M26.
* Bất kỳ task FAIL → BLOCKED (fail-closed, T001 Rule 7).
* Mọi run có provenance (T001 Rule 5).
* Cùng closure → cùng verdict (deterministic).
* Báo cáo được coverage toàn bộ milestone.
* Tích hợp được với AIOS 2.0 Coding Integration + Regression Gate + Benchmark/Regression + Evidence.
* Regression của các milestone trước PASS; không vi phạm invariants.

### 6. Dependency / Gate

```text
TASK-217 AIOS 2.0 Coding Integration
TASK-001 Task Governance System (Rule 7)
TASK-033 Benchmark + Regression
        ↓
TASK-218 Full M0–M26 Regression
```

TASK-218 là **task cuối của toàn bộ roadmap M0–M26** — hoàn thành Coding Completion Contract: `AUTHORIZED → EXECUTED → VERIFIED → RESILIENT → GOVERNED → EVALUATED → CERTIFIED`.

### 7. Test Matrix

| Scenario | Expected |
| -------- | -------- |
| toàn bộ closure PASS | all_pass=true |
| một task FAIL | BLOCKED (fail-closed) |
| cùng closure | cùng verdict (deterministic) |
