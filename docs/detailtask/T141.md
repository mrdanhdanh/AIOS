## TASK-141 — Output + Artifact Collector

**Mục tiêu**

Triển khai **Output + Artifact Collector** như một năng lực có contract, evidence và harness riêng — thu thập output (stdout/stderr/log) và artifact từ test/build/lint run (T139/T140) thành artifact có provenance. TASK-141 là **collector, không phải runner mới** (dựa trên Test Runner T139 + Build/Lint T140 + Coding Artifact T130 + Evidence T001).

TASK-141 thuộc **M20**, ngay sau T139/T140. Trọng tâm canonical: **Output + Artifact Collector**.

### 1. Phạm vi — Collector, không new runner

```text
Output + Artifact Collector
├── Output Capture       (stdout/stderr/log)
├── Artifact Collect     (từ T139/T140)
├── Hash + Provenance    (T078/T001)
└── Policy Boundary       (T113)
```

* **Output Capture** — thu stdout/stderr/log từ run.
* **Artifact Collect** — thu artifact từ test/build/lint (T139/T140).
* **Hash + Provenance** — mọi artifact có `content_hash` (T078) + provenance (T001 Rule 5).
* **Policy Boundary** — collector không lộ secret (T040/T113).

### 2. Collector Contract

```text
CollectedArtifact
├── run_ref              (T139/T140)
├── outputs[]            (stdout/stderr, content_hash)
├── artifacts[]          (content_hash, T130)
├── collector_id         (immutable, T001 Rule 1)
├── policy_ref           (T113)
└── evidence_ref         (T001)
```

Quy tắc:

* Mọi output/artifact có `content_hash` (T078) + provenance (T001 Rule 5).
* Collector không lộ secret (T040/T113).
* Cùng run → cùng collected set (deterministic).
* Mọi collect có provenance (T001 Rule 5).

### 3. Collector Safety

* **Fail-closed** — output/artifact không hash được → reject (T078).
* **Secret isolation** — không lộ secret (T040/T113).
* **Evidence required** — mọi collect có provenance (T001 Rule 5).
* **Deterministic** — cùng run → cùng collected set.
* **No parallel collector** — dùng Test Runner (T139) + Build/Lint (T140) + Coding Artifact (T130) + Evidence (T001).

### 4. Deliverables

* Output Capture (stdout/stderr/log).
* Artifact Collect (từ T139/T140).
* Hash + Provenance (T078/T001).
* Policy Boundary (T113).
* Integration với Test Runner (T139) + Build/Lint (T140) + Coding Artifact (T130) + Evidence (T001) + Integrity (T078).
* Unit + Contract + Integration + Architecture + Regression tests.
* ADR/docs (bắt buộc — collector).

### 5. Acceptance Criteria

TASK-141 chỉ PASS khi:

* Collector thu output (stdout/stderr/log) từ run.
* Collector thu artifact từ test/build/lint (T139/T140).
* Mọi output/artifact có `content_hash` (T078) + provenance (T001 Rule 5).
* Collector không lộ secret (T040/T113).
* Output/artifact không hash được → reject (fail-closed, T078).
* Cùng run → cùng collected set (deterministic).
* Tích hợp được với Test Runner + Build/Lint + Coding Artifact + Evidence + Integrity.
* Regression của các milestone trước PASS; không vi phạm invariants.

### 6. Dependency / Gate

```text
TASK-139 Test Runner
TASK-140 Build / Lint Runner
        ↓
TASK-141 Output + Artifact Collector
        ↓
TASK-142 Verification Engine
```

TASK-141 collector; T142 verification xây trên collected.

### 7. Test Matrix

| Scenario | Expected |
| -------- | -------- |
| capture output | stdout/stderr + hash OK |
| collect artifact | từ T139/T140 OK |
| output không hash được | reject (fail-closed) |
| collect secret | không lộ (T040) |
| cùng run | cùng collected set (deterministic) |
| collect evidence | provenance đầy đủ (T001) |

### 8. Definition of Done

```text
PLAN → SPEC → CRITIQUE×2 → BREAKDOWN → REVIEW
→ IMPLEMENT → TEST → EVALUATE → REGRESSION → PROGRESS/LOG → COMMIT
```
