## TASK-182 — Trust Lifecycle + Invalidation + Selective Reverification

**Mục tiêu**

Triển khai **Trust Lifecycle + Invalidation + Selective Reverification** như một năng lực có contract, evidence và harness riêng — vòng đời trust (issue→active→invalidate→reverify), fail-closed. TASK-182 là **trust lifecycle, không phải certificate mới** (dựa trên Governance Ledger T181 + Trust Evaluator T164 + Certification T049 + Ecosystem Trust T046 + Evidence T001).

TASK-182 thuộc **M24**, ngay sau T181. Trọng tâm canonical: **Trust Lifecycle + Invalidation + Selective Reverification**.

### 1. Phạm vi — Trust lifecycle, không certificate mới

```text
Trust Lifecycle + Invalidation + Selective Reverification
├── Lifecycle            (issue→active→invalidate→reverify)
├── Invalidation         (thu hồi trust)
├── Selective Reverify   (reverify chọn lọc)
└── Policy Boundary      (T113)
```

* **Lifecycle** — vòng đời trust có state machine (T001 Rule 6).
* **Invalidation** — thu hồi trust khi evidence invalidate (T181/T078).
* **Selective Reverify** — reverifier chỉ phần affected (T164).
* **Policy Boundary** — lifecycle qua policy (T113).

### 2. Trust Lifecycle + Invalidation + Selective Reverification Contract

```text
TrustLifecycle
├── trust_id             (immutable, T001 Rule 1)
├── ledger_ref           (T181)
├── state                (ISSUED/ACTIVE/INVALIDATED/REVERIFYING)
├── invalidated: bool
├── evidence_ref         (T001)
└── authority: aios
```

Quy tắc:

* Evidence invalidate → trust INVALIDATED (fail-closed, T078/T181).
* Mọi transition có provenance (T001 Rule 5).
* Lifecycle deterministic: cùng input → cùng state.
* Selective reverify chỉ chạy phần affected (T164).

### 3. Trust Lifecycle + Invalidation + Selective Reverification Safety

* **Fail-closed** — evidence invalidate → INVALIDATED (T078/T181).
* **Immutable id** — `trust_id` không tái sử dụng (T001 Rule 1).
* **Evidence required** — mọi transition có provenance (T001 Rule 5).
* **Deterministic** — cùng input → cùng state.
* **No parallel lifecycle** — dùng Governance Ledger (T181) + Trust Evaluator (T164) + Certification (T049) + Ecosystem Trust (T046) + Evidence (T001).

### 4. Deliverables

* Trust Lifecycle + Invalidation + Selective Reverification (lifecycle + invalidate + reverify).
* Policy Boundary (T113).
* Integration với Governance Ledger (T181) + Trust Evaluator (T164) + Certification (T049) + Ecosystem Trust (T046) + Evidence (T001).
* Unit + Contract + Integration + Architecture + Regression tests.
* ADR/docs (bắt buộc — trust lifecycle + invalidation + selective reverification).

### 5. Acceptance Criteria

TASK-182 chỉ PASS khi:

* Trust lifecycle có state machine (T001 Rule 6).
* Evidence invalidate → INVALIDATED (fail-closed, T078/T181).
* Selective reverify chỉ chạy phần affected (T164).
* Mọi transition có provenance (T001 Rule 5).
* Cùng input → cùng state (deterministic).
* Tích hợp được với Governance Ledger + Trust Evaluator + Certification + Ecosystem Trust + Evidence.
* Regression của các milestone trước PASS; không vi phạm invariants.

### 6. Dependency / Gate

```text
TASK-181 Governance Ledger + Provenance Graph
TASK-164 Trust Evaluator + CodingCertificate + Verification Harness
TASK-049 Certification
TASK-046 Ecosystem Registry (Trust)
        ↓
TASK-182 Trust Lifecycle + Invalidation + Selective Reverification
        ↓
TASK-183 Approval Workflow + Rollback Recommendation
```

### 7. Test Matrix

| Scenario | Expected |
| -------- | -------- |
| evidence valid | ACTIVE |
| evidence invalidate | INVALIDATED (fail-closed) |
| cùng input | cùng state (deterministic) |
