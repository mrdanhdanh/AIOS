## TASK-140 — Build / Lint Runner

**Mục tiêu**

Triển khai **Build / Lint Runner** như một năng lực có contract, evidence và harness riêng — chạy build/lint trong sandbox (T136) + workspace (T137) dưới policy (T138), sinh result có provenance. TASK-140 là **build/lint runner, không phải contract mới** (dựa trên Execution Contract T135 + Sandbox T136 + Workspace T137 + Policy T138 + Evidence T001).

TASK-140 thuộc **M20**, ngay sau T136/T137/T138. Trọng tâm canonical: **Build / Lint Runner**.

### 1. Phạm vi — Build/lint runner, không new contract

```text
Build / Lint Runner
├── Sandbox Dispatch     (chạy trong T136)
├── Workspace Use        (dùng T137)
├── Policy Enforce       (qua T138)
└── Result + Evidence    (T001)
```

* **Sandbox Dispatch** — chạy build/lint trong sandbox (T136).
* **Workspace Use** — dùng workspace/snapshot (T137).
* **Policy Enforce** — mọi build/lint qua policy (T138).
* **Result + Evidence** — result có provenance (T001 Rule 5).

### 2. Build/Lint Runner Contract

```text
BuildLintRun
├── execution_ref        (T135)
├── sandbox_ref          (T136)
├── workspace_ref        (T137)
├── policy_ref           (T138)
├── build_results[]      (pass/fail, content_hash)
├── lint_results[]       (pass/warn/fail, content_hash)
└── evidence_ref         (T001)
```

Quy tắc:

* Build/Lint chỉ chạy trong sandbox (T136) + workspace (T137).
* Vi phạm policy (T138) → BLOCK (fail-closed, T078).
* Mọi result có `content_hash` (T078) + provenance (T001 Rule 5).
* Cùng target + cùng env → cùng result (deterministic).

### 3. Build/Lint Runner Safety

* **Fail-closed** — policy vi phạm → BLOCK (T078).
* **Sandbox only** — không chạy ngoài sandbox (T136/T040).
* **Evidence required** — mọi result có provenance (T001 Rule 5).
* **Deterministic** — cùng target + env → cùng result.
* **No parallel runner** — dùng Execution (T135) + Sandbox (T136) + Workspace (T137) + Policy (T138) + Evidence (T001).

### 4. Deliverables

* Build/Lint Runner (chạy build/lint trong sandbox).
* Workspace integration (T137).
* Policy Enforcement (T138).
* Result + Evidence (T001 Rule 5).
* Integration với Execution Contract (T135) + Sandbox (T136) + Workspace (T137) + Policy (T138) + Evidence (T001).
* Unit + Contract + Integration + Architecture + Regression tests.
* ADR/docs (bắt buộc — build/lint runner).

### 5. Acceptance Criteria

TASK-140 chỉ PASS khi:

* Build/Lint Runner chạy build/lint trong sandbox (T136) + workspace (T137).
* Vi phạm policy (T138) → BLOCK (fail-closed, T078).
* Mọi result có `content_hash` (T078) + provenance (T001 Rule 5).
* Build/Lint không chạy ngoài sandbox (T136/T040).
* Cùng target + env → cùng result (deterministic).
* Tích hợp được với Execution + Sandbox + Workspace + Policy + Evidence.
* Regression của các milestone trước PASS; không vi phạm invariants.

### 6. Dependency / Gate

```text
TASK-136 Sandbox Manager
TASK-137 Workspace / Snapshot Manager
TASK-138 Resource + Network + Command Policy
        ↓
TASK-140 Build / Lint Runner
        ↓
TASK-141 Output + Artifact Collector
```

TASK-140 build/lint runner; T141 collector xây trên result.

### 7. Test Matrix

| Scenario | Expected |
| -------- | -------- |
| chạy build trong sandbox | OK, result có hash |
| chạy lint trong sandbox | OK, result có hash |
| policy vi phạm | BLOCK (fail-closed) |
| chạy ngoài sandbox | bị chặn (T136/T040) |
| cùng target + env | cùng result (deterministic) |
| build/lint evidence | provenance đầy đủ (T001) |

### 8. Definition of Done

```text
PLAN → SPEC → CRITIQUE×2 → BREAKDOWN → REVIEW
→ IMPLEMENT → TEST → EVALUATE → REGRESSION → PROGRESS/LOG → COMMIT
```
